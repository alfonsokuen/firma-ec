---
import { getEntry, render } from 'astro:content';
import ArticleSeo from '../layouts/ArticleSeo.astro';

const entry = await getEntry('pages', 'es/que-es-firma-electronica');
if (!entry) throw new Error('Missing content/pages/es/que-es-firma-electronica.md');
const { Content } = await render(entry);
const data = entry.data;

const articleProps = {
  title: data.title,
  description: data.description,
  routeKey: 'que-es-firma-electronica',
  h1: data.h1 ?? data.title,
  datePublished: data.datePublished,
  ...(data.dateModified !== undefined ? { dateModified: data.dateModified } : {}),
  breadcrumbs: data.breadcrumbs,
  related: data.related,
};
---
<ArticleSeo {...articleProps}>
  <Content />
</ArticleSeo>
