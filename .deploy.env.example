# Coordenadas del despliegue. Copiar a .deploy.env (ignorado por git) y rellenar.
# NO versionar .deploy.env: este repo tiene espejo publico.

# Nodo manager del Swarm, alcanzable por SSH como root.
IAS_HOST=

# Registry Docker interno, en formato host:puerto.
REGISTRY=
