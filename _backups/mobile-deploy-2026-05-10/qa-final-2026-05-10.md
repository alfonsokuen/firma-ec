# QA + Auditoría final — 2026-05-10 post-deploy

**Scope:** validar que las 6 tareas del turno (F7.6 + F7.5 + F8 spec + cold-cache + repro-builds + SLSA-doc + mobile UX) están LIVE, sin regresiones, y la postura de seguridad/perf no se degradó.

## Estado servicios

```
firma-ec_landing   2/2 (max 1 per node)   firma-ec-landing:v0.1.13
firma-ec_pwa       2/2 (max 1 per node)   firma-ec-pwa:0.7.0-rc4

Tasks Running:
  firma-ec_landing.1   IASERVER02   17 min uptime
  firma-ec_landing.2   IASERVER01   18 min uptime
  firma-ec_pwa.1       IASERVER02   14 min uptime
  firma-ec_pwa.2       IASERVER03   14 min uptime
```

4/4 réplicas saludables, distribuidas en 3 nodos (IAS01+IAS02+IAS03), sin tareas en `Failed` o errores en CurrentState.

## API regression — todos los gates funcionan

| Endpoint | Método | Resultado | Esperado |
|---|---|---|---|
| POST `/api/ocsp/securitydata` | application/ocsp-request | 200 | proxy → upstream |
| POST `/api/ocsp/argosdata` | application/ocsp-request | 200 | proxy → upstream |
| GET `/api/crl/securitydata` | — | 200 | proxy → upstream (~1.19MB) |
| GET `/api/crl/argosdata` | — | 200 | proxy → upstream (~7.5KB) |
| GET `/api/ocsp/unknown` | — | **404** | allowlist gate (anti-SSRF) |
| GET `/api/crl/unknown` | — | **404** | allowlist gate |
| GET `/api/ocsp/evil%2eevil` | — | **404** | path traversal protection |
| POST `/api/ocsp/securitydata` | text/plain (wrong CT) | **404** | matcher gating |
| GET `/api/ocsp/securitydata` | — | **404** | matcher gating (POST only) |
| GET `/api/tsa` | — | **405** | F6 regression invariant |
| POST `/api/tsa` | application/timestamp-query | 200 (FreeTSA responded "Bad request" a payload basura) | proxy alive |

## Performance — 3 cold runs por origen

```
firmar.ec/
  run1: total=0.836s ttfb=0.544s tls=0.093s size=122KB http=200
  run2: total=0.476s ttfb=0.296s tls=0.020s size=122KB http=200
  run3: total=0.419s ttfb=0.253s tls=0.018s size=122KB http=200

app.firmar.ec/
  run1: total=0.449s ttfb=0.449s tls=0.083s size=3.7KB http=200
  run2: total=0.229s ttfb=0.229s tls=0.017s size=3.7KB http=200
  run3: total=0.226s ttfb=0.226s tls=0.017s size=3.7KB http=200
```

Cold TTFB peor: **544ms** (firmar.ec run1, primera resolución DNS+TLS). Subsiguientes: <300ms. Todos pasan Core Web Vitals "Good" (TTFB <800ms) con margen.

## Compresión / negotiation

```
assets/index-J1FOYTmW.js
  br   → 55,049 bytes
  gzip → 64,081 bytes
```

Brotli activado, 14% más eficiente que gzip. Cliente con Accept-Encoding="br" obtiene la versión correcta.

## Security headers (https://app.firmar.ec/)

```
strict-transport-security: max-age=31536000; includeSubDomains; preload
content-security-policy: default-src 'self'; script-src 'self' 'unsafe-inline' 'wasm-unsafe-eval';
                         style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:;
                         font-src 'self'; connect-src 'self' https://ocsp.firmar.ec https://freetsa.org;
                         worker-src 'self' blob:; object-src 'none'; base-uri 'none';
                         form-action 'self'; frame-ancestors 'none';
                         require-trusted-types-for 'script'; upgrade-insecure-requests
cross-origin-opener-policy: same-origin
cross-origin-embedder-policy: credentialless
x-frame-options: DENY
x-content-type-options: nosniff
referrer-policy: no-referrer
permissions-policy: accelerometer=(), camera=(), geolocation=(), microphone=(), … web-share=(self) …
service-worker-allowed: /
x-robots-tag: noindex, nofollow
```

CSP incluye solo los upstreams legítimos para F7.5 (`'self'` cubre /api/ocsp/* y /api/crl/* via mismo origen). FreeTSA y CF Worker mantienen scope explícito.

## Bundle integrity — los 3 cambios mobile fueron desplegados

```
✓ 3 ocurrencias de "h-11 w-12" en assets/index-J1FOYTmW.js
  (esperadas: TSA toggle + LT toggle + LTA toggle = 3)
✓ Regla CSS .h-11 definida en assets/index-Cj8_qIxZ.css
  (.h-11{height:calc(var(--spacing) * 11)} = 44px)
✓ p12.worker chunk referenciado desde assets/crypto-utils-KGhb3fJe.js
  (dynamic import via Vite Worker constructor)
✓ Zoom controls (5/5 strings):
   - i-lucide-zoom-in
   - i-lucide-zoom-out
   - "Reducir zoom" (aria-label)
   - "Aumentar zoom" (aria-label)
   - "Ajustar al ancho" (zoom reset title)
```

## Auditoría visual (Playwright iPhone 14 + Pixel 7)

Cubierta a inicio del turno (antes del crash del MCP browser que ocurrió post-rc3 deploy). Resultados:

| Surface | Viewport | Horizontal overflow | Touch targets <44px (acción primaria) | LCP |
|---|---|---|---|---|
| firmar.ec / | 390×844 | none | 0 (post-fix) | 481ms |
| firmar.ec / | 412×915 | none | 0 (post-fix) | (similar) |
| app.firmar.ec / | 390×844 | none | 0 | 944ms |
| app.firmar.ec /#/firmar | 390×844 | none | 0 | — |
| app.firmar.ec /#/verificar | 390×844 | none | 0 | — |
| app.firmar.ec /#/configuracion | 390×844 | none | **rc3=1 / rc4=0** ✓ | — |
| app.firmar.ec /#/paranoia | 390×844 | none | 0 | — |
| app.firmar.ec /#/acerca | 390×844 | none | 0 | — |

> Nota: el remaining "3 touch targets <44px" reportado por route corresponden a **inline body links legítimos** (header logo "firmar.ec app", footer credit "IDKMANAGER", footer "security.txt") — son texto, no controles primarios, y bumping a 44px les daría aspecto desproporcionado. Conservados intencionalmente.

## Gaps conocidos / NO regresión

| # | Item | Estado |
|---|---|---|
| 1 | Playwright MCP browser crashed post-rc3 | Sin recovery este turno; auditoría visual previa válida para rc4 (sólo cambió el 3er toggle, ya verificado vía bundle inspection) |
| 2 | F8 §9 7 preguntas abiertas | Esperando decisión user (stack/chip/hosting/pricing/branding/cedula/esfuerzo) |
| 3 | Branch protection main repo | Decisión política pendiente (solo-maintainer hoy) |
| 4 | Reproducible-builds verify bit-idéntico | Quick wins aplicados (SOURCE_DATE_EPOCH + tar deterministic + gzip -n); diffoscope verify pendiente CI job |
| 5 | 15/17 ACEs ARCOTEL PEMs | Outreach manual (CA-by-CA) |
| 6 | F7.5 PWA proxy en producción | LIVE ✓ verificado este turno |
| 7 | Cold-cache landing | 334ms LCP (23× bajo el 7.7s del concern original) |

## Resultado QA

**PASS — sin regresiones, los 6 deploys del turno funcionan correctamente.**

- Mobile UX cadena (a+b+c+touch) LIVE y verificada
- F7.5 OCSP/CRL proxy LIVE con allowlist working
- F7.6 fix shipped en rc2+ (test desbloqueó CRL SecurityData de 30,842 revoked certs)
- F6 TSA regression intacto
- Security headers + CSP + COOP/COEP estables
- Performance Core Web Vitals "Good" tier mantenido

**Rollback ready** (independientes):
```bash
# PWA
docker service update --image 190.160.10.129:5000/firma-ec-pwa:0.7.0-rc2 firma-ec_pwa
# Landing
docker service update --image 190.160.10.129:5000/firma-ec-landing:v0.1.12 firma-ec_landing
```

## Commits del turno (cronológico)

```
8383715 fix(ltv): F7.6 raise asn1js maxNodes for real ARCOTEL CRLs
5a00445 feat(ltv): F7.5 same-origin proxy for OCSP/CRL upstreams (allowlisted)
cfc4be1 docs(F8): spec draft v0.1 — multi-firmante con flujo orchestration
44ec5c2 docs(audit): cold-cache re-audit closed — 334 ms LCP
c79e4a5 chore(supply-chain): reproducible builds quick wins + SLSA conformance doc
a8cb5db deploy(pwa): F7.5 LIVE — same-origin OCSP/CRL proxy active in production
4097a0a feat(mobile): p12 decrypt off main thread + zoom controls + 44px touch targets
919a13f fix(configuracion): LTA toggle h-7→h-11 (3rd toggle missed by replace_all)
2334d27 deploy: mobile improvements LIVE — pwa 0.7.0-rc4 + landing v0.1.13
```

Todos pusheados a Gitea (mirror automático a github.com/idkmanager + github.com/alfonsokuen).
